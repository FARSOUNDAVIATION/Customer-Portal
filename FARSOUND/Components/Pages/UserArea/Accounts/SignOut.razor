@using Microsoft.AspNetCore.Authentication

@page "/userarea/accounts/signout"

<PageTitle>FARSOUND | Signed out</PageTitle>
<h3 style="text-align: center;">Signed Out</h3>

<div class="background">
    <br />
    <br />
    <h1 style="text-align: center; font-weight: bold;">Customer Portal</h1>
    <br />
    <br />
    <br />
    <br />
    <p style="text-align: center;">You have successfully logged out of the FARSOUND Customer Portal. Check out our <a href="https://www.farsound.com/">website</a>.</p>
</div>

@* <button @onclick="SignOutUser" style="display: block; margin: 0 auto;">Logout</button> *@

@code {
    private async Task SignOutUser()
    {
        // Perform sign out logic here, e.g., clearing authentication tokens, etc.
        // await AuthenticationService.LogoutAsync();

        NavigationManager.NavigateTo("/Home"); // redirect to the home page after sign out
    }

    [Inject]
    private NavigationManager NavigationManager { get; set; }

    [Inject]
    private IAuthenticationService AuthenticationService { get; set; }
}
